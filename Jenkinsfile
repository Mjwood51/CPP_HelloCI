//Commit before test

//Here is another revision

pipeline {
    agent { dockerfile true } }
    stages {
        stage('build') {
            steps {
                sh 'cpp --version'
            }
        }
    }
}

